version: '3'

services:
  app:
    image: adrianobree/webacademy:latest
    container_name: webacademydemo
    restart: always
    ports:
      - "8080:8080"
    environment:
      NODE_ENV: production